export function user(state = null, action) {
    switch (action.type) {
        default:
            return {
                name: 'Paul',
                avatar: 'img/avatar/005.jpg'
            };
    }
}
