{
  "description": "A UI/UX prototype for Chipi desktop client",
  "main": "main.js",
  "scripts": {
    "start": "npm run styles & electron .",
    "styles": "postcss styles/styles.css --output styles.css --watch",
    "browser": "npm run styles & http-server .",
    "prettier": "prettier --single-quote --tab-width=4 --print-width=120 ./*.js ./src/**/* --write"
  },
  "repository": "https://github.com/oscarotero/chipi-client",
  "private": true,
  "devDependencies": {
    "electron": "^4.0.0-nightly.20181010",
    "http-server": "^0.11.1",
    "postcss-cli": "^6.0.1",
    "postcss-extend": "^1.0.5",
    "postcss-import": "^12.0.1",
    "postcss-preset-env": "^6.4.0",
    "prettier": "^1.15.2"
  },
  "dependencies": {
    "redux": "^4.0.1",
    "redux-thunk": "^2.3.0"
  }
}
